# Used only by the Prisma CLI during development (`npm run prisma:migrate`,
# `npm run prisma:generate`). The packaged app never reads this file — at
# runtime the database path comes from Electron's userData directory
# (see src/main/config.ts) and is passed straight into the Prisma client.
DATABASE_URL="file:./dev.db"
